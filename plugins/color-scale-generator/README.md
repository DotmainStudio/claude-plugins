# color-scale

Generates a complete 0–100 color scale from any HEX color using HSL seed-relative interpolation.

Invoke with: `color-scale` skill — triggers when user asks to decompose a color, generate a palette, or create UI tokens from a HEX.

Outputs: 17-step scale, semantic tokens (bg, text, border, accent), full 0–100 scale.
